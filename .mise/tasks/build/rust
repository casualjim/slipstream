#!/usr/bin/env bash

set -eu -o pipefail

#MISE description="Build Slipstream"
#MISE sources=["**/Cargo.toml", "**/*.rs"]

echo "🚀 Starting Slipstream Rust 🦀 build..."
proj_dir=$(git rev-parse --show-toplevel)
cd "$proj_dir" || exit 1

cargo build --release --workspace "$@"
echo "✅ Slipstream Rust 🦀 build complete!"
